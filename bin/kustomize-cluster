#!/bin/bash

CLUSTER_NAME="$1"

if [ -z "$CLUSTER_NAME" ]; then
  echo "No value given for positional argument #1 CLUSTER_NAME. Aborting."
  exit 1
fi

kustomize build \
  --enable-alpha-plugins \
  --load-restrictor LoadRestrictionsNone \
  "deployments/clusters/$CLUSTER_NAME/"
